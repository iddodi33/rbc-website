# Decisions Log

One line per entry. Add one every time either of you overrides the other's agent, changes something in CLAUDE.md, ROADMAP.md, or ARCHITECTURE.md, or makes a call that isn't written down anywhere else. Newest entry at the top.

Format: `YYYY-MM-DD — who — what was decided — why`

## Log

- 2026-09-05 — Iddo — RESOLVES THE OPEN HERO-TYPE ITEM. The headline goes to --paper, not --accent, and the two-tone split the colour was carrying is now weight plus opacity: "Community first." stays 700 solid, "Basketball second." is 600 at 0.72 opacity, both --paper. 0.72 is the value --text-invert-muted already uses for secondary text on ink, so it is the site's existing quiet-text step rather than a new one. The eyebrow went to --paper as well, which the original OPEN item also covered: over the huddle photograph --accent reaches 4.26:1 at 1280 and 1.54:1 at 360, and even --accent-tint stops at 4.26:1 at 360, so neither is usable. There is now no accent anywhere on the hero. That is a consequence of measurement, not a preference — accent still carries every other surface on the site, where it passes. The scrim is KEPT, because the headline still fails without it (2.61:1 at 1280, 2.94:1 at 360), and its curve was retuned from 0.30 at 42% to 0.45 at 45% with the bottom from 0.72 to 0.76: the huddle has a pale wall across the top and on the 4:5 phone crop the eyebrow lands in the old dip at 4.28:1. Curves up to 0.62 at the midpoint were tested and 0.45 is the GENTLEST that clears 4.5:1 everywhere, so it darkens the photograph least. Worst case anywhere on the hero is now 5.57:1
- 2026-09-05 — Iddo — SAME FINDING, SECOND PLACE: the current-page nav marker is --accent, and over a photograph it fails too — 1.42:1 to 2.11:1 on the five band images WITH their scrim, 2.64:1 on the home hero. It now goes --paper, underline included, but ONLY while `.site-header.is-over-hero`; on every solid surface the marker stays accent, where it passes. The underline matters as well as the text: as a state indicator it owes 3:1 under WCAG 1.4.11 and accent misses that too. Written out per page rather than keyed on aria-current, because aria-current comes from nav.js and the visible marker has to survive without JS, exactly as the rule it overrides does
- 2026-09-05 — Iddo — NARROW BAND on story, roster, sponsors, shop and contact, replacing `.page-head`, which was a title on paper with an accent rule under it and is now deleted. Full bleed, same negative top margin as the hero so it slides under the transparent header, page title over the photograph. Deliberately a strip and not a second hero: clamp(14rem, 32vh, 20rem) against the hero's clamp(30rem, 78vh, 46rem), which measures 288px against 712px at 1280 and 237px against 577px at 360, about 40% either way. The band <img> is absolutely positioned on purpose — left in flow it contributes its own intrinsic height (1280 / 3.2 = 400px) and the min-height never gets a say, so the strip quietly grew to 56% of the hero. The home hero has the same in-flow arrangement and the same quirk, and was left alone because this branch was not asked to change the hero's geometry. Worth fixing separately
- 2026-09-05 — Iddo — Band scrims decided PER PAGE, not globally, as instructed — and all five turned out to need one. Measured title contrast against each page's own photograph, worst 8px tile, --paper title: bare 1.18 / 1.72 / 1.14 / 1.17 / 1.63 at 1280 for story, roster, sponsors, shop, contact, and 1.20 / 1.85 / 1.22 / 1.48 / 1.70 at 360. Every one of them is essentially invisible bare, because all five are bright halls. Scrimmed they are 5.06 / 6.59 / 5.18 / 4.99 / 6.16 and 5.46 / 7.08 / 6.26 / 5.89 / 6.57. The band scrim is a slightly different curve from the hero's (0.42 at 45% rather than 0.45) because the band is a third of the height and the title sits lower in it. If a band photograph is swapped, re-measure that page — the class is per page precisely so that a dark photograph can drop it
- 2026-09-05 — Iddo — The header-transparency observer moved from an inline IIFE on index.html into js/nav.js, and now watches `.hero, .page-band`. The earlier entry kept it inline because only the home page had a hero and shipping it to all six pages, including Phil's, would have been dead weight. Every page now opens with a full-bleed photograph, so that reason is gone and six copies of the same IIFE would be worse. Supersedes that entry
- 2026-09-05 — Iddo — Individual photographs go INSIDE the page bodies, never at the top, so the band above stays the page's one picture. player-portrait on roster, sponsor-portrait on sponsors, contact-portrait on contact, jersey-front and jersey-back as a pair on shop. The sponsors choice is the load-bearing one: that photograph has the Neighbourhood Threat wordmark fully legible on the jersey, which is what a sponsor's money actually buys, photographed rather than described in a tier table. The shop pair is front and back of the SAME jersey — Neighbourhood Threat on the front, Blackbird Rathmines on the back — so between them they show the whole garment. Portraits are capped at 24rem and landscapes at 40rem: uncapped, a 2:3 portrait in a 1200px container renders 1800px tall and becomes the second hero these are specifically not
- 2026-09-05 — Iddo — Iddo's agent edited shop.html (Phil's area) and sponsors.html (shared, normally ask first) on the `heroes` branch, on Iddo's explicit instruction. Scope was strictly the band, the photographs and the shared footer — no cart, Stripe, Supabase or sponsor-tier content was added or touched, and each page carries a comment saying so. Logged here because the ownership table in CLAUDE.md says to
- 2026-09-05 — Iddo — The "Come and watch" band gets NO photograph. `home come and watch.jpg` was the obvious candidate and turned out to be the same huddle as the new hero, a wider 3:2 crop of it, so it would have repeated the top of the page; the file is deleted rather than kept as a near-duplicate. Nothing was substituted either: that band is the one place on the site that asks for a single specific action, and a photograph beside the button competes with it and pushes the link down on a phone. If one is ever wanted there it should be spectators or the bar afterwards, which is what the copy describes, not more basketball
- 2026-09-05 — Iddo — Instagram handle confirmed as @rathmines_bc and now live in four places: the "Come and watch" button, the handle under it, the fixtures fallback sentence, and the shared footer on all six pages. Outbound links, target=_blank and rel=noopener, with the handle visible as text rather than buried behind "Message us" so it can be read and searched for without following a link. Phil still owns the Instagram EMBED and the rest of the social links; this is only the club's own account
- 2026-09-05 — Iddo — [CLOSED 2026-09-05, see the `heroes` entries at the top of this log] HERO CONTRAST, MEASURED ON THE REAL PHOTOGRAPHS, and one failure left open. `hero--scrim` is now APPLIED on index.html: the white headline line measured 2.21:1 at 1280 and 2.06:1 at 360 against --paper without it, and 5.41:1 and 4.85:1 with it, so it fails the 4.5:1 bar bare and passes scrimmed. The nav/header strip goes 2.11:1 and 3.85:1 bare to 9.01:1 and 10.48:1 scrimmed. BUT the two accent-coloured elements on the hero still fail and a scrim cannot fix them: "Basketball second." measures 1.04:1 bare and 1.86:1 scrimmed, and the eyebrow 1.71:1 bare and 2.64:1 scrimmed, against #C1682D. The reason is that #C1682D is a mid orange and so is this gym — to put accent text at 4.5:1 the backdrop has to sit within a hair of pure black, which means roughly a 90% scrim and no photograph left. So the fix is either a different hero photograph or paper-coloured type on the hero, and neither is decided here — the brief was to add the scrim if the headline failed, and that is all that was done. OPEN FOR IDDO. Method: worst 8px tile behind each element, scrim composited, measured on the exported files rather than in the browser; the sampler was checked against the known --accent-on-black value of 4.64:1 before use
- 2026-09-05 — Iddo — The two Rathmines photographs sit under the club copy on the home page, directly below the venue address, and deliberately NOT in the hero. The hero has one job, saying that this club plays games, and a clock tower and a terrace of houses say "neighbourhood" instead, which is the wrong first thing and would displace the player photograph. Under the address they do what the address alone cannot: the clock tower is on the same Lower Rathmines Road as the venue and the houses are the Rathmines the members live in, so "Rathmines, Dublin 6" stops being a line of type. The `.place` grid is 4fr / 9fr at 768px and up, which is not a taste call: the clock is 2:3, the street is 3:2, and 4:9 is those two ratios in proportion, so both run at native aspect ratio, finish at the same height (measured 494.5px and 494.9px, a sub-pixel rounding difference) and neither is cropped. Swap either photograph for a different shape and the ratio has to be recomputed
- 2026-09-05 — Iddo — The CC BY-SA 2.0 photo credit lives in the SHARED footer, on all six pages, not under the photographs. The licence obliges us to name photographer and licence visibly wherever the work is shown, and the shared footer is the one block that is byte-identical across the site, so the credit cannot drift out of sync or be lost if the photographs move. Both the photographer and the licence are real links, underlined rather than accent-coloured so they read as links without competing with the footer nav. It is not display:none, not a comment and not alt text, and it must not be removed while those files are in assets/img/
- 2026-09-05 — Iddo — Hero exports are now JPG, not PNG: the three filenames in the <picture> element were updated and the two flat-black PNG placeholders deleted, along with the `.hero__placeholder-label` rule and its span, which had nothing left to mark. The hero <img> also gained real alt text — it was `alt=""` while it was a flat black placeholder, which was correct then and would now hide a real photograph of the club from anyone using a screen reader
- 2026-09-05 — Iddo — Promotion history on the home page corrected and expanded. The old line said only that "the men's team" went up and plays Division 6, which was incomplete on two counts. Both men's teams were promoted last season, D5 to D4 and D7 to D6, and two squads took a playoff trophy, the men's D4 team winning the D5 playoff and the women's team winning the D2 playoff. Promotion and playoff are separate mechanisms and the page now says so in a footnote: promotion is decided on final league position, the playoff is a standalone knockout between the top four teams in a league, a semi-final and a final. Neither causes the other, so no copy may present the playoff win as the reason for a promotion. The women were NOT promoted — they played Division 2 last season and play Division 2 this season — and nothing on the site may imply otherwise. The trophies have no competition name of their own beyond the league's, they are the Division 5 and Division 2 playoffs, and no name is to be invented for them. CLAUDE.md carries the same facts as a hard rule
- 2026-09-05 — Iddo — The promotion band grew from one sentence to a heading plus three squad entries plus a footnote, so it now uses the standard `.section` padding and the shared `.section__head` + `.eyebrow` + `h2` vocabulary like every other band, and the `.promotion` rule is deleted outright — a plain `h2` inside `.section__head` already gives the right size and the right 34rem measure, so the class had nothing left to say. Each squad entry reuses the venue block's accent `border-inline-start` rule rather than becoming a card, keeping "a stated fact" a single visual motif. `.section--tight` was its only user and is deleted rather than left as dead CSS
- 2026-09-04 — Iddo — Training is closed to newcomers, so the site carries no training times and no open-session invitation anywhere. The whole public path is: message the club, come and watch a game, have a chat and a beer with the team afterwards — and it is written to read exactly that warmly and that plainly. Nothing on the site may say "come down", invite anyone to a session, or otherwise read as an open door to practice. This is the opposite of the reference site (plymouthbasketball.co.uk), which is a youth club built around open bookable sessions: its layout language was borrowed, its content model deliberately was not
- 2026-09-04 — Iddo — Fixtures live in `fixtures.json` at the repo root and are rendered client side by a small inline script on index.html, no build step and no framework. Every date in the source is null: the league has not published them, so all dates render as TBC, the list is deliberately NOT sorted by date, and nothing may imply one. Home games render first and are shown by default with away games behind a per-squad toggle — that is both the fix for 18 rows per squad on a phone and the point of the block, since home games are the ones people can come to. Two caveats carried from the data's own `notes`: the D6 list is `verified: false` because the division header was cropped in the source screenshot and is D6 only on Iddo's say-so, and one D6 away fixture has no venue on the source page and renders as "Venue not listed" rather than blank. Women's D2 was added to the file as a division with an empty fixtures array so "Fixtures to follow" comes from the data rather than from a special case in code
- 2026-09-04 — Iddo — roster.html on the `roster` branch is built for four squads (D4 men, D4 women, D7 men, D7 women) and is now wrong. It will be rebuilt for men's D4, men's D6 and women's D2 after this branch merges, so that it is built once against the final design rather than twice — do not merge `roster` before then
- 2026-09-04 — Iddo — Roster structure corrected to three senior squads, one team per division: men's D4, men's D6, women's D2. D6 replaces D7 throughout, because the men's team was promoted. There are no separate men's and women's teams within a division — supersedes the "D4 and D7" and "men's and women's" wording in CLAUDE.md, ARCHITECTURE.md and ROADMAP.md, all three now corrected, including the `roster` table's division field which holds D4, D6 and D2. The earlier "Team & Roster covers D4 and D7" entry below is left untouched as the historical record

- 2026-09-04 — Iddo — Typefaces are Archivo (display: headings, nav, buttons) and Source Serif 4 (body), loaded from Google Fonts via `<link>` — no typeface was specified anywhere in the docs; Archivo gives the tight athletic "chalk" half and Source Serif the warm "leather" half. Google Fonts keeps the no-build-step constraint. Revisit if a cookie/GDPR banner becomes necessary, since it is a third-party request — self-hosting the two `.woff2` files is the fallback and needs no other change
- 2026-09-04 — Iddo — Added a fifth colour token, `--accent-ink` #8F4A1E, for small text and links on paper only — #C1682D on #F7F4EF is 3.62:1, which fails the WCAG AA 4.5:1 body-text threshold. #8F4A1E is 6.05:1. It is a text-only darkening of the accent, not a new brand colour: nothing visual (fills, rules, borders, display type) may use it, and on #141414 the plain accent is 4.64:1 and passes, so this token is not needed there
- 2026-09-04 — Iddo — Nav label for roster.html is "Roster", not "Team" — matches the filename, so there is no ambiguity for Phil or for a later find-and-replace
- 2026-09-04 — Iddo — The site header and footer are black (#141414) bars — the supplied club mark is white artwork on an opaque black background, not a transparent PNG, so it needs a dark surface to sit on. `mix-blend-mode: screen` knocks the black box out against the bar. If a transparent or paper-background version of the logo is ever produced, this can be revisited
- 2026-09-04 — Iddo — No templating engine: the header and footer are duplicated byte-identically across all six pages, and the current-page marker is driven by a `data-page` attribute on `<body>` rather than per-page nav markup — keeps the shared blocks a single find-and-replace, and keeps the marker working without JS. A generator script produced the six files once and was deliberately not committed, since committing it would amount to a build step
- 2026-09-04 — Iddo — The `/supabase/functions/` folders from ARCHITECTURE.md were NOT created in the shell branch — those two functions are Stripe/Shop work, which is Phil's area, and this session was scoped to static only. Git cannot track empty directories anyway, so creating them would have meant committing placeholder files into his area. Phil creates them on his branch

- Pre-launch — Iddo & Phil — Stack is static HTML, CSS, and JS plus two Supabase Edge Functions, no app framework — only two things in the whole build need server-side code, a framework would add complexity with no benefit
- Pre-launch — Iddo & Phil — Hosting is Vercel — carried over from an earlier "Vercel is fine" answer, flip to GitHub Pages here if that changes
- Pre-launch — Iddo & Phil — Merch orders relay to teamwear.ie manually, no automated email to their inbox — keeps the process manual until there's a proven market, and we don't yet know how they want to receive orders
- Pre-launch — Iddo & Phil — Sponsor tiers exist (3) but are not priced or published at launch — website and branding come first, tier pricing and the pitch document come later
- Pre-launch — Iddo & Phil — Iddo builds the shell solo, merged before Roster or Shop start — avoids two competing design systems forming in parallel
- Pre-launch — Iddo & Phil — Leather and Chalk palette locked: black #141414, paper #F7F4EF, accent #C1682D, tint #EFD3B4 — ties the basketball identity to something warmer without losing the black and white anchor
- Pre-launch — Iddo & Phil — Team & Roster covers D4 and D7, seniors only, no underage teams — confirms no safeguarding or GDPR question on publishing roster data
